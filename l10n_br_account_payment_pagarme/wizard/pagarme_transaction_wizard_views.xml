<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Pagar.me Transaction Wizard -->
    <record id="pagarme_transaction_wizard_form" model="ir.ui.view">
        <field name="name">pagarme.transaction.wizard.form</field>
        <field name="model">pagarme.transaction.wizard</field>
        <field name="arch" type="xml">
            <form string="Pagar.me Transaction Operation">
                <sheet>
                    <group>
                        <group>
                            <field name="operation" required="1"/>
                            <field name="transaction_id" required="1"/>
                        </group>
                        <group>
                            <field name="amount" 
                                   attrs="{'invisible': [('operation', '!=', 'refund')], 'required': [('operation', '=', 'refund')]}"/>
                            <field name="reason" 
                                   attrs="{'invisible': [('operation', 'not in', ['cancel', 'refund'])]}"/>
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button name="action_execute_operation" 
                            string="Execute" 
                            type="object" 
                            class="btn-primary"/>
                    <button string="Cancel" 
                            class="btn-secondary" 
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action for Pagar.me Transaction Wizard -->
    <record id="action_pagarme_transaction_wizard" model="ir.actions.act_window">
        <field name="name">Pagar.me Transaction Operation</field>
        <field name="res_model">pagarme.transaction.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>